

describe("Chat Window", () => {
  it("should show an online user as being online", () => {
    const expected = true;
    // const component = mount(<ChatWindow userList={users} />);
    // const actual = component.find('')
    expect(true).toEqual(expected);
  });
});
